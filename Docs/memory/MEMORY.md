# Memory Index

This directory is the canonical memory for the project. Read this index at the start of a
session. Notes go here, scratch files go in `Docs/scratchpad/` — see the workspace conventions.

Working instructions (build, tests, porting rules, style) are in `AGENTS.md` at the repo root.

## Working practice

- **Traces you can switch on in game** (all off/cheap by default): `SimCopter.People.Trace` +
  `TraceStates` + `TraceOpcodes` follow one person's behaviour program — selections, goto-object
  steps, what refused them, outcomes, despawns ([the move core note](simcopter-people-move-core-cell-rules.md));
  `SimCopter.Riot.Log` follows a riot's lifecycle ([crime/riot](simcopter-crime-rooftop-rescue.md));
  `SimCopter.Dispatch.MarkerLog` follows a dispatch pylon's emissive write
  ([vehicle material](simcopter-vehicle-material.md)). Reach for one before theorising — the last
  three times a behaviour bug was guessed at, the trace disagreed.

- [Agent workspace conventions](agent-workspace-conventions.md) — memories and scratch files live IN THE REPO (`Docs/memory/`, `Docs/scratchpad/`), never in an agent's machine-local dirs.
- [Building and running](build-and-run.md) — always compile with `RebuildUnrealCpp.bat` at the repo root (it pins `-NoLiveCoding`); feed it empty stdin or its trailing `pause` hangs.
- [Building and running on macOS](mac-build-and-run.md) — git-lfs, the Metal toolchain, `RebuildUnrealCpp.sh`, Clang's stricter warnings, and why Mac packaging needs `-package` and SM5 shaders.
- [Mac performance](mac-performance.md) — measured on an M1: GPU-bound, volumetric clouds are ~44 ms of a 102 ms Epic frame; `Tools/MacBench` harness, its traps, and what shipped.
- [SimCopter Ghidra workflow](simcopter-ghidra-workflow.md) — MAIN PATH: ghidra-bridge instant queries over `.ghidra-exports`; re-agent for parity/LLM loops; analyzeHeadless only for scan/bytes/decompileforce. Use `dump-asm` whenever the value you want is a stack-built struct (dialog rects, font sizes) — the decompile aliases those slots, the assembly does not.
- [SimCopter live memory rip](simcopter-live-memory-rip.md) — read the live process with ReadProcessMemory for `.data` ONLY (SimCopterX relocates `.text` in memory; read code from Ghidra); per-region `.data` calibration.
- [Editor MCP](editor-mcp.md) — the running editor is queryable/drivable over MCP (`Docs/EditorMcpWorkflow.md`). The config is `SimCopterRemake/.mcp.json`, in the **.uproject folder** — a client started at the repo root loads no tools at all; `Tools/Unreal/McpCall.ps1` is the raw-HTTP fallback. Everything hides behind `call_tool`, schemas are strict, and never guess an object's `refPath`.
- [Packaging the Shipping build](simcopter-packaged-build.md) — faults a standalone-from-editor run cannot show: maps/assets addressed by name are invisible to the cook, media must be loose NonUFS, and the six required original-data trees must be staged from repo `Reference` through Build.cs runtime dependencies plus the `[Staging]` remap. Read `Manifest_*Files_Win64.txt` before theorising; playable packages contain `<package root>/SimCopter/{bmp,cities,geo,sound,tweak,x}` automatically.
- [SimCopter in-game verification](simcopter-ingame-verification.md) — RARELY: only for a problem a build + automation test cannot settle (AGENTS.md §7). How-to if it is: launch `-game -windowed`, drive/screenshot the Slate UI from PowerShell, poll the log for readiness; centred panels shift and stale click coords silently no-op.

## File formats and city data

- [SimCopter building footprints](simcopter-building-footprints.md) - `FUN_0047c0c0` ignores XZON for mesh ownership; claim the square size from the XBLD footprint table in row-major scene-cell order. The old XZON owner shifted Islandtown's large GEO models onto roads.
- [SimCopter mesh orientation rules](simcopter-mesh-orientation-rules.md) — the original uses no per-tile mesh rotation; the col axis is negated in world placement. `FUN_004814c0`'s five-frame water texture cycle hard-switches at a debug-tunable 4 Hz on the dedicated terrain-water surface, separately from WPO waves; 4000 is a fixed-time threshold, not milliseconds. Mesh pools/ponds remain on static authored atlas cells, terrain water binds its MID before render-section creation, and coplanar mesh-water faces retain a constant 2 cm render-only lift.
- [SimCopter privanim DECODED](simcopter-privanim-decoded.md) — exact DF container spec; 21 named figures incl. Elvis/Nessie; ARPP = per-frame line segments; the OLD display rules were wrong; `people.df` = same container.
- [SimCopter terrain flattening](simcopter-terrain-flattening.md) — FUN_004abce0 tmap conditioning decoded+ported: flatten under buildings/flat roads, +0x20 ramps under raised spans 0x3f-0x42, water dip; single raster sweep, order matters.
- [SimCopter instanced buildings](simcopter-instanced-buildings.md) — buildings are per-model runtime UStaticMesh instances (103 models / 2624 placements), not baked into the merged city mesh, so one can be removed when it burns down. **Trees have their own collision object channel since 2026-08-12** (`SimCopterFoliage`) so only the helicopter hits one — and the half that mattered was dropping them out of `ECC_Camera`, which is what the pedestrian walk/step/spawn probes trace on. **Trees + the park are instanced too since 2026-08-05** (8 models / 3250 placements, merged mesh 509k -> 434k tris) but by a SEPARATE path with STATIC components — a movable city-sized primitive is what the virtual shadow map cannot cache. The park (LP13) is now flattened like a building; the trees still follow the slope. **SIM3D page 2 cell 0 is a Maxis debug PORTRAIT** (2026-08-07) and is now dropped: four objects put it on a sign board — CO182's billboard and a front/back pair each on CO124/126/127 — and the shipped game never shows it. Scratchpad has `label_sim3d_page.py` / `list_atlas_cell_users.py` for answering "which cell is that".
- [SimCopter airport spawn](simcopter-airport-spawn.md) — FUN_004829f0 STAMPS the airport over the SC2 city (XBLD 0xf6 + 0xde) and levels the 5x5 corner patch; finding the block is only half the port, and there is no separate "first helicopter" placer.
- [SimCopter road decorations](simcopter-road-decorations.md) — `FUN_0047c0c0` hangs a SECOND object off nine road cases: bins/phones/hydrants/post boxes on odd/odd straight roads (litter bin is 7 rolls in 16), `LAMP35..38` at every fourth T junction, `SIGNAL1` at crossroads (0x2b unconditionally). **The props carry their own in-tile offset**, so they place at the road slab's own tile origin — measure with `dump_maxis_bounds.py` before inventing one. A street light's cone is eighteen face-type-11 quads plus a ground pool, and the remake's spot light is measured off them. **Traffic signals get cards but NO point lights on purpose** (the whole world strobes by colour at 20 Hz). Chimney smoke is face-type-26 class-1 markers on IN160/162-165/192 + PP202/207, drawn by the same `FUN_00496da0` kernel as the fire and dropped until now because a 1-vertex face is not a polygon. **Two same-day traps:** road tiles set `bBuildVectorLines = false` and the decoration inherited it (that is the missing arm between a signal's pole and its box), and the lamp's painted face-11 cone ENCLOSES the spot light's apex, so the light was there and invisible. Street lights are the one local light Low Power keeps — check `bLowPowerMode` in GameUserSettings before calling missing headlights a regression.
- [SimCopter road tile variants](simcopter-road-tile-variants.md) — every road tile has a flat `RD*L` mesh and a curbed sloped one; the heuristic XBLD table demotes the `L` suffix, so the remake put a curb on every street. All placed road/ramp/bridge cells cache their authored asphalt plane for smooth vehicle Z and pitch, while GEO face-type-20 lines render directly on that plane. Trains use directional low/half/high rail-slope edges plus `FUN_004b7020`'s exact 31/64-tile RL90/RL90F lift. Composite bridge towers/supports are never selected as driving surfaces. Also lists the unported road decorations (incl. SIGNAL1).

## Flight and the helicopter

- [SimCopter heli flight model DECODED](simcopter-heli-flight-model.md) — physics/controls/rotor decoded+ported as `FSimCopterFlightModel` (16.16); traps: tenth-deg units, speed = smoothed pitch, SlideRate (not PitchRate) ramps pitch keys, rotor gate 300, fps-capped EMA. **Easy model ported 2026-07-29** — `DAT_00503aa0` is the VIEW mode, not a difficulty flag; only the pitch ramp is halved, not slide. **Landing fixed 2026-07-30** — the neutral-collective surface push fires on NOT-flat ground; the remake had it inverted. **All per-frame rules converted to delta time** the same day against `OriginalFrameSeconds` (0.05 s) — incl. the attitude EMA, whose fps clamp never stopped the filter running more often; turbulence needs its injection scaled too, or interpolating makes it worse.
- **The engine loop must be stopped when the player gets out** (2026-08-25, one line in `ExitHelicopter`). `UpdateHelicopterAudio` is the only thing that stops `COPLOOP` — at the rotor's 30 gate — and it returns early the moment the pawn stops being locally controlled, so nothing is left watching. `CanExitHelicopter` wants the skids down, NOT a stopped rotor, so getting out right after landing stranded a 2D loop at full volume that followed the player around the city for the rest of the session. The blades still wind down on their own (the pawn keeps ticking, the engine is already off on touchdown, and `FUN_00487740`'s parked branch takes the rotor to zero). **A ground idle was built for this and then removed on 2026-08-25 — do not rebuild it.** Worth knowing from that attempt: the loop does not go quiet as the rotor decays, because `SetFrequencyHz` clamps pitch at 0.4, so `FUN_00488fd0`'s law bottoms out around 250 rpm and holds one steady swish down to the cut-off; and `Play2D` ignores an already-playing slot while `Play3D` re-aims one, which is why a running 2D loop cannot simply be moved onto the airframe.
- [SimCopter helicopter collision + impact](simcopter-helicopter-collision.md) — the original has ONE collision system and it NEVER blocks: a height test plus `FUN_0048ad50`'s per-tile AABB overlap, both answering with damage + a bounce that throws the airframe clear. A blocking collider in the remake pins the helicopter (and the blocked position used to be written back into the flight model, so the sim believed it too). Gate the impact on `LandingFlatNormalZ`, not a wall-ish normal, or slopes hit the skids and do nothing; play the audio AFTER `ApplyFlightModelToActor` or `Step()` wipes the event first; and put the burst at `Hit.ImpactPoint` un-submerged. **Object damage must be packeted at `CollisionSubtract` (27)**: the executable's 4 HP is one 20 Hz overlap frame, while the remake emits one 0.2-second-rate-limited swept impact; copying 4 literally made the starting Schweizer require 124 hits instead of 19. Career difficulty does not scale collision damage. Fire damage is `FUN_004a5c10`'s 24-unit BOX around each flame — the flight model's fire arm was dead code because nothing ever set `FireHeightDelta`. **The root capsule is a 190 cm SPHERE** (`InitCapsuleSize` clamps the half height up to the radius) sized for that sweep — never the answer to "where is the helicopter" for anything on foot: since 2026-08-05 boarding measures `GetDistanceToAirframeCm`, the rendered fuselage's own box, and the sphere overlaps pawns instead of blocking them. Do not resize it; its half height is the altitude datum. **People are neither obstacles nor ground** (same day): both downward surface traces skip `ECC_Pawn` hits — a pedestrian's head was the landing surface, since `heli[0x59]`'s trace starts above the aircraft — and only state-10..13 criminals answer for a collision, via `FUN_0049a4f0(0xc)`'s BHAV 912 plus a divergent `EVT_CriminalCaught`. **A wreck now returns to an airport pad** (2026-08-05) instead of repairing where it fell — the pad registry the stale comment said did not exist is the one city entry uses — plus a remake-only 15 s watchdog for a death spiral whose ground never arrives.
- [SimCopter heli tools/models](simcopter-heli-tools-models.md) — tools+registry decoded; traps: tear gas is interaction mode 5 not 7, stowed flags are 1=raised and the rope node counts DOWN, FUN_00489250 is the spotlight not a downwash disc, runtime type order != twk/shop order. **Tear gas ported 2026-07-31** — the muzzle (the old `heli[0x57]` open item) is the body node +3.0 up along the airframe's own forward axis at `heli[0x4e] + 50.0`, not the spotlight's aim; the canister's node is never rotated and `node+0x10` is a collision radius; the burst and the first gas puff share a frame; class 0x8 bounces off walls and passes through people; BHAV 907 + 311 turn puffs of gas into riot dispersals. **Apache corrected 2026-08-01:** tracers render from their own constant-velocity slots; impact spheres (missile 150 cm, gun 50 cm) turn nearby cars/people into exact car-fire/medevac mission targets while retaining the original direct-hit reactions and missile-only building-fire path.
- [SimCopter water gameplay](simcopter-water-gameplay.md) — the bucket never douses directly; particle impact does, at strength = remaining life; a water cannon exists and is unported.
- [SimCopter cockpit flaps](simcopter-cockpit-flaps.md) — tool flaps decoded; the click-box table sits in an UNANALYZED Ghidra gap (scan `.text` for pointer refs); flapbtn frames are unequal widths. **The flap tick is in that gap too, and both readouts are now ported** (2026-07-31): `0x00455300` polls `career+0x54` and `heli[0x74]` every fourth frame and repaints flap3's ten canister lamps (from flapbtn1's mystery 4px sliver — that is what it is for) and flap0's eleven-cell water bar (`watergge.bmp` = full | meniscus | empty, 5x10 each, from page (16,43)). flap3 ships with its lamps lit so only spent ones are painted; flap0 ships EMPTY with the meniscus printed at x 16, so the gauge must draw all three states over it. **Every cockpit bar is one of these three-cell strips** — dash6's points bar is `managge.bmp`, fifteen 5x13 cells from (20,37); drawing the whole bitmap as one block is how it shipped wrong.

## Population, traffic and agents

- [SimCopter walking sounds](simcopter-walking-sounds.md) — `FUN_004c71c0` assigns shoes/heels/boots as each person's own looping voice; `FUN_004c6970` starts/stops it with the `NoMo`/`1Wal`/`1Run` selector, and the player pilot uses `xFtBoots.wav`. Ported as attached, speed-paced loops independent of dialogue. The player keeps a six-tile cutoff; NPCs use full volume, 800 cm range, and are gated by actual horizontal velocity so stopped/blocked people are silent.
- [SimCopter people logic](simcopter-people-logic-next.md) — the big one. **The behaviour VM ticks on a fixed 0x147a (0.08 s) accumulator, not once per frame (2026-08-11)** — 10-12.5 Hz depending on frame rate, so the remake's 15 Hz is already faster than retail and tick-count-to-seconds conversions must use 12, not 15 or 20. Traps: dispatch table 0x58ef78 is SPARSE (opcode numbering!), the PRNG is a left-shift xor, figures bind by behavior class at spawn (dog=10/cow=17/Elvis=20), walk anims come from the post-move selector. **Opcode table finished 2026-07-29** — every shipped record site is ported (UFO abduction, street chats, riot contagion, fire gawking); op 35 is a collapse-into-casualty, not a despawn. **Riot instant-completion fixed 2026-08-01:** opcode 24 cannot fail on a calm crowd (`FUN_004c9f10` is `void`), and `FUN_004c4190` seeds spawn-mode-3 rioters to the exact agitation value 7. A valid riot owns at least 11 real people and waits for their outcomes. **The riot tools were all dead and it was ONE bug (2026-08-12):** `FUN_004c1050`'s 903/915/912/909 priority rule is gated on `person+0x15c`, which is the **player's-cabin seat count** (`FUN_004c6250`/`FUN_004c62e0`), so it governs passengers only and a street person accepts every reaction, always — the port had read `person+0x17c` as a never-cleared "reaction running" latch, so the first bump-driven BHAV 914 in a crowd made every rioter permanently deaf to the megaphone (901), tear gas (907) AND water cannon (908). The only repeat throttle is "this program is already the top STACK frame", and `person+0x15e` "written off" is the real guard that keeps reactions off the dying.
- [The move core's per-cell rules](simcopter-people-move-core-cell-rules.md) — `FUN_004c9470` brackets EVERY tile rule in "did this step change cell", and a walk tick is ~5 cm against a 400 cm tile, so it almost never does. Testing them per step froze a Robber on the spot. Also: `DAT_0058d750` is not in the move core at all, non-ambient walkers have NO class test, `FUN_004c9cc0`'s water gate is the rule the class rows had been faking, and **+0x168 is written by the shipped BHAVs** (600 sets it, 1400/1401 clear it). **The goto-object walk on top of it (`FUN_004ca940`) is in there too**: only a clean step continues a walk, autoturn is off by default so it aims one facing and takes what it gets, and the arrival gate's vertical half must measure the drawn body — a car's traffic capsule put the doorsill 33.75 cm underground against a 31.25 cm gate, which is what froze the policeman and the robber at their car.
- [Cars hit people](simcopter-vehicle-knockdown.md) — WHOLE-CLOTH DIVERGENCE (2026-08-11), there is no decompile behind it: retail traffic and pedestrians pass straight through one another (the blocking probe only sees vehicles, the move core only sees people). A moving car now launches an ambient walker proportionally to its own speed and exaggerated, takes nothing itself and raises no mission; the "ragdoll" is one rigid `VisualRoot` turn because a privanim figure has no skeleton. Walls come from an `ECC_Camera` sphere sweep, the ground from `TraceGround` (never the sweep — the walk probe is what knows where a person may stand), water ends the tumble. 1 s sprawled → 3 s in the authored `Dead` pose → up; anyone who lands in the sea wades to the nearest land tile instead. **Nobody is exempt** — mission casualties, medics and criminals all get launched, because the tumble SUSPENDS their program and hands it back at the new position rather than restarting them; the only people skipped are the ones not standing in the street (riders, carried bodies, UFO catches, mid-fall passengers), and a carrier appearing mid-tumble cancels it above `Tick`'s carried early-returns. Measure the car's rendered BODY, not its 33.75 cm traffic capsule. **Also fixes a long-standing art bug it made obvious: every lying pose was drawn INTO the ground** — figure calibration pins Z=0 to the STANDING clip's feet and nothing held the others above it, so `Inju` buried a Child 19.6 cm of a 44 cm body (head only showing). Lift is measured against the walk cycle's own dip, not zero, because `Calibrate` reads frame 0 and every shipped walk swings 2-3 cm below it. **The airframe does it too** (after the decoded criminal run-over, which keeps first claim), with its own launch angle and its own horizontal-only minimum ground speed so landing/hovering/taxiing among a crowd stays safe. Four live knobs on the debug panel's `KNOCKDOWN`/`airframe` rows (`SimCopter.Knockdown.*`), CVars because a per-instance UPROPERTY only moves one of hundreds of agents. **Also holds the road rule**: `FUN_004c9470`'s ambient arm admits only `DAT_0058ec00` classes and NO row there contains class 7, so retail crowds never step off the kerb — but the remake's gate for it (`AmbientFlag`, +0x168) is never written, so everyone took the loose "safety net" branch and the whole population walked down the middle of the road. Restored, with a per-sim 20% / 10 s jaywalk window as the one hole (`RoadJaywalkChance`); roads are also an ambient walker's only route between blocks. **A walk step is ~8 cm against a 400 cm tile**, so the rule must only govern STEPPING OUT into the road — applied to somebody already standing in one (a car threw them there) it refuses all eight facings and freezes them for tens of seconds.
- [SimCopter traffic jam queue](simcopter-traffic-jam-queue.md) — `FUN_0049ee30`/`FUN_0049be50`: a blocked car **does not move at all** (no nudge, no lane change, no reverse — the remake's bypass recovery is an invention), and **only same-direction traffic blocks** (blocking radius scales by `(1+dot)/2`, the probe wants `dot >= 0.5`, so oncoming cars are ignored and a queue cannot swap places). Hold distance = both radii + 5 units = 98.75 cm. The pile-up was three systems at once: following never reaches zero (0.18 creep), overlap resolution split the push *and* added an impulse, and the stoplight queue aims every car at one shared stop line. `TrafficFlowMode::TrafficJam` is dead — never assigned. **Cars now drive around rioters (DIVERGENCE, 2026-08-12):** `ApplyRioterAvoidance` slows on a look-ahead corridor using *this note's* jam braking (`ApplyTrafficBrake` at `NormalTrafficBrakeRate`, crawl floor above the 0.18 creep), steers by the smallest clear angle, commits one short `SetAvoidanceMoveTarget` hop at a time, and lets that hop's expiry hand the car back to the road — advancing to the next node when the old one ended up behind it. Retail cars cannot see people at all.
- [SimCopter population rendering](simcopter-population-rendering.md) — car headlights are face-type-11 beam cards; people aren't in GEO (use a procedural box body); ground agents must Camera-trace from high up or they hover.
- [SimCopter UE figure component](simcopter-ue-figure-component.md) — privanim figures render in UE; ARPP Z is y-down (negate!); unity builds disabled in `Build.cs`; engine at `C:\GameDev\UE_5.8`.
- [SimCopter ambient vehicles](simcopter-ambient-vehicles.md) — planes/trains/boats + the four crash/rescue missions ported; plane slot 1 IS the UFO, boat slot 0 is CAPBOAT1, Plane/Train Crash tuning is bound-but-dead, a plane ditching on water makes a boat rescue, and only cities with rail (e.g. 12) can show a train. **The Apache-vs-UFO fight (`FUN_004b3ba0`) was dead three ways and was ported 2026-08-06**: the ambient meshes are `NoCollision` so the weapon trace missed them, `ResolveImpact` would have counted an aircraft as terrain, and `HitCount` was never incremented. **Only missiles damage the UFO** (ten of them); machine-gun fire just kills its lights, and either weapon downs a civilian airliner in one for a −100/−$200 penalty. **CAPBOAT1 is modelled UPRIGHT** (2026-08-07) — the capsize is a 180° roll the renderer applies, now folded into `SetBoatMeshTransform` so none of the three placement sites can miss it — and **anyone standing on a water tile now sinks to the waist over 0.25 s and rides the swell**, visually only (`UpdateWaterSubmersion` on the ground agent and the on-foot pawn): the capsule stays on the rest plane because every height gate the sim measures against a person is a small decoded number.
- [SimCopter passenger display + person heads](simcopter-passenger-display.md) — the seat manifest (`DAT_005040d0+0x1d4`) and its blit decoded+ported. **Heads are per behavior class, and head 10 (the bandage) belongs to state 6 alone**. A mission behavior class of -1 must run `FUN_004c7190`, not fall through to class 0; portrait row comes from BHAV 264's health/damage-scaled-speed graph. Opcodes 57/85 drive the patient's EKG and cries. Runtime-observed damaging impacts apply one BHAV 281 deterioration quantum and immediately re-tune the EKG; an ordinary passenger's scared impact face is transient and yields back to BHAV 264 after BHAV 292's 13-tick / 0.87 s polling period. Entering the destroyed state runs `FUN_004c0ba0(1)` and writes off everyone aboard. PEOPLE1 palette index 254 must become zero-RGB transparent black, and passenger cells use a distinct nearest-neighbor brush so the cyan key cannot bleed into a teal outline.
  - **Portrait dragging fixed 2026-08-09:** never pass a null brush to the passenger drag operation. Hide the source without collapsing it, size the decorator from its geometry, preserve the cursor grab offset, and let the outer seat well handle a return drop without changing the manifest.

## Missions, dispatch and effects

- [Pacing divergences: mission clock, arsonist cadence, base slot](simcopter-pacing-divergences.md) — **the places this port deliberately does NOT reproduce the original** (2026-08-11). Every tier now gets tier 1's 600 s mission timer / 75 s nag, because retail's scaling compounds with the higher mission cap and reads as jobs expiring on arrival. And the arsonist throws on a 50-100 s timer with an eligible site always igniting, instead of `rand(1000) < 6` then `1 in (8 - tier)` — which measured out at a fire every half hour. And (2026-09-23) the Base Location record is left out of the scheduler's mission cap, which retail counts, leaving tier 1 with one job at a time. Do not "fix" either back by citing the decompile.

- [SimCopter mission system (M5)](simcopter-mission-system.md) — scheduler/fire/lifecycle; plan at `Docs/Milestone5SimulationPlan.md`; the old 0x20/0x40 event-mask guesses were wrong; the mission layer uses MSVC rand, not the people LFSR. Fire sim decoded+ported (flames CLIMB and re-arm; +0x0c is a growth step not a size).
- [SimCopter passenger-mission failure rules](simcopter-passenger-mission-failure.md) — **the transport timeout is REAL and lives in the BHAV, not the walker (2026-08-25)**: `FUN_004a73e0` has no expiry arm for rescue/transport/medevac, but `BHAV 290 'Transport increment boredom, possibly disappear'` rolls 1-in-5 for `attr35 += 1 + tier` and at >100 posts outcome 11 (`EVT_PassengerLost`, +0xbc) and despawns the fare — so an ignored transport fails on a clock (~1.7 min tier 1) and resolves net-negative to failure voice 0x60. Rescue has no give-up at all; medevac fails by death (BHAV 280→312). **Boredom only runs while WAITING** — op12 is what hands 750 off to BHAV 292, so a retail fare in the cabin can never be lost. Also: the nag is gated on **pickedUp**, not delivered (`JMP` at 004a7678/004a7829 skips it once everyone is aboard), medevac never nags, nags dock points only, record base is 0x57f9dc. **Both remake bugs fixed 2026-08-25**: the nag now reads pickedUp (transport clears the TERTIARY marker, rescue/medevac the primary), and `StepTowardSelectedObject` reports Arrived when the selection already IS your carrier — which is what stopped a fare seated by `PickUpMissionPeopleNear` (BoardCarrier never advances the program) from sitting in 291's boredom loop and bailing out in mid-flight. Match the harness flag there or the seat rebuilds every tick.
- [SimCopter emergency dispatch](simcopter-emergency-dispatch.md) — F2-F5 decoded+ported; traps: Ghidra mis-types FUN_004bc680 (read the asm), the "message id" IS the body's GEO object id, FUN_0042de60(1) is the Shift key, svc 3 vs 4 is the initial STATE not a count.
- [SimCopter paramedic handoffs](simcopter-paramedic-handoffs.md) — **a medevac has NO destination (2026-09-23): `FUN_004a7a10`'s 0x20 branch never writes +0x30, and any hospital (XBLD D1) takes the patient; the remake now serves every hospital roof and hands off at whichever one the player lands on.** ambulance vtable deploy is class 12/state 5, not criminal-car `(0xf,0xd)`; BHAV 262 seeks/totes victims to object class 10 ambulance, while BHAV 263 unloads the real helicopter patient at XBLD D1 without a synthetic doorway. The hospital roof post is rate-limited (40 s): a boarding medic stops matching the scan, and backfilling on the next tick spawned a duplicate. **Roof paramedics overhauled 2026-07-31** — the pedestrian-node builder still owned footprints by XZON, so 22 of 30 career cities (Islandtown included) had NO hospital spawn site at all; the walker never advances past a stop opcode, so refusing a persistent medic's despawn parked them on it forever (refusals must `ResetToState`); **a medic taking the patient off the helicopter now credits the delivery wherever it happens** (BHAV 282's XBLD-209 gate soft-locked the mission when the handoff was anywhere else); and the post is confined to its own roof on the transform, because crowd separation and traffic impulses move agents with no walked-surface check. **"Arrived at an object" is CONTACT, fixed 2026-08-05** — `FUN_004ca940`'s move result 10 is `FUN_004c8f70`'s body-vs-body box overlap at the step target, tested BEFORE the tile/climb/bump rules and returning without displacing; the remake accepted "same tile", and a tile is 400 cm. **But the snatch was never in the BHAV graph:** a 45 s mission-side watchdog (`AdvanceMedevacHandoff` -> `DeliverMedevacDirectly`) teleported the patient out of the cabin with no medic involved, so tightening the walk made it FIRE MORE. That path is abstract seats only now, ops 44/47 gate on airframe reach, and a posted roof crew ignores an aircraft that is not over its building. **The street ambulance loop fixed 2026-08-05, and two of the three faults were self-inflicted:** `BoardCarrier` keyed the toted-body pose off "carrier is a ground agent", so a medic that boarded its own ambulance was laid across its flank in the `Dead` clip (it takes `bAsCarriedBody` now); and the "unresolved mission person" despawn refusal caught **emergency crew**, who never set `bMissionResolutionReported` because that flag belongs to passengers - so the medic was reset into BHAV 801 while attached and move-suspended, forever. Third: a vehicle's contact extent was the **33.75 cm traffic capsule** on a body ~90 cm long, so `FUN_004c8f70`'s box overlap now measures the rendered body. **Do not re-add a medic-in-cabin healing mechanic - BHAV 281 already is one**, and it is ported: the drain roll is 1-in-20 with a hidden state-5 medic aboard, 1-in-10 without, 1-in-3 on an unserviceable ground tile, `-= 1 + tier` when it hits. **Instant boarding fixed the same day: a live guidance target SKIPS the VM's movement branch in `UpdateMovement`**, so `GuideMissionPeopleToLocation` (every tick, 780 cm) had passengers beelining in at the generic 230 cm/s while BHAV 750→291 wanted 2.67 s of waiting then 125 cm/s octant walking from one tile, on a re-rolled `rand(40)+30` budget with a wave/idle/boredom loop. Guidance is a 20 s stall backstop now, radius 400, at the shipped speed. **And `Wave` != `WvNo`** — both ship in every figure; `Wave` is the panic clip (rioter flee/run, Rxn: Ouch, Fireman), `WvNo` is the greeting BHAV 291 rec[4] actually uses. **The level-complete marching band is BHAV 443/444** (states 17/18, figure TubaExpert), ported 2026-08-05 to replace an invented script: it binds `'Play'` not a wave, the LEADER plays march.wav (sound 38) while MEMBERS toot one of nine instrument samples (sound 37), and `attr29` (person+0x17a, the state's program id, now written by `ResetToState`) is what tells them apart. **The airport is tile class 1 and no behaviour row allows it** — 0xde/0xf6 both fall through `GetTileClassForBuildingId` to its catch-all — so a walker spawned on the apron is frozen; the band spawns on walkable tiles outside the block. **Getting OUT is six units and it is polled, fixed 2026-08-06:** `FUN_004c9bc0` wants the person within 6 units (37.5 cm) of the ground, and a rider's Y IS the aircraft's (`FUN_004c6450` copies the carrier's position on every tick), so that is `GroundClearanceCm` directly — while a landed heli is only 1.2 units up (`Altitude = TerrainHeight + 0x13333`). Boarding is a DIFFERENT, looser number (op 12: 8 units / 50 cm), so the one 88 cm gate is now `PassengerAlightClearanceCm` + `PassengerBoardClearanceCm`; and BHAV 292/700 only probe every ~13 ticks, so the mission-side release waits `PassengerAlightSettleSeconds` instead of firing mid-descent. **The roof medic's march to the parapet is the same day:** BHAV 801's `Walk-10` is on the ENTRY PATH ONLY (52 cm, autoturn off, once in the original's short life), and the remake's despawn refusal `ResetToState`s it about once a second — replaying that one-shot along a facing nothing re-rolls. Restart now re-rolls the facing, and an aimless walk is capped at `HospitalRoofPostIdleWanderFraction` of the post while a seeking one keeps the whole roof.
  - **The doropn cue is a TRANSITION, not a call (2026-08-13):** `BoardCarrier`'s early-out only catches an exactly-matching re-board, so a caller that toggles the harness flag (op 12 picking the rope end one tick and the cabin the next) tears the seat down and rebuilds it EVERY TICK — re-notifying the mission layer, resetting the portrait and re-firing voice event 60. That is "the get-in sound played over and over". Gate the cue on not having already been in that cabin, and refuse a harness board for somebody already seated.
  - **Passenger drops fixed 2026-08-08:** gravity must advance even when `TraceGround` cannot yet see a landing surface, and `PassengerFallStartZ` must be latched at release before that trace or tall falls freeze and/or undercount their impact distance.
- [SimCopter crime, riot, and rooftop rescue](simcopter-crime-rooftop-rescue.md) — exact masks/titles, scheduler and placement, BHAV phases, recurring Burglar car, riot erosion, roof-aware victims, rewards/penalties, and voice ids. **Riot tracing added 2026-08-12** (`LogSimCopterRiot` / `SimCopter.Riot.Log`, plus `SimStartRiot`), **and it found why riots end in seconds**: BHAV 852's `riotValue = count * mean / 15` has its fixed point at exactly **count 15 — sixteen people in the 3x3 tile block** — so any smaller crowd walks its own agitation down 7→2 in ~8 s and BHAV 311 retires it, and a tier-1 riot only asks for 9-16. Second, separate bug now fixed: rioters posted their leave outcome **repeatedly** (29 events from 11 people) because the "unresolved mission person" despawn refusal caught them — `bMissionResolutionReported` belongs to passengers, the third time that trap has bitten. RiotSize is the SPAWNED count; `TryGetRiotCentroid` is dead. **Confirmed and acted on the same day:** an 11-strong riot probes `count 9, mean 7 -> target 4` and dies in 7.5 s, a 16-strong one probes `count 15` and holds at the agitation ceiling indefinitely — so riot size is now floored at `MinimumViableRiotSize` (16) for both the request and the acceptance test, and a riot may only seed on people tile class 7, because state 3 walks on roads alone and a riot placed in open country freezes on its spawn point. Both are documented divergences covered by `SimCopter.Missions.RiotViability`. **Arsonist fires are rare by design, not by port bug (2026-08-11):** ~1 throw per 7 min of unobstructed arsonist and then 1-in-(8-tier); the firebomb carries event id -1 so only the "create a new fire" arm exists, water never douses it, and the effect tables are runtime-initialised so the decompile aliases them. `SimArsonFirebomb` makes the whole chain checkable in seconds. **Rooftop delivery fixed 2026-08-09:** Rescue/Transport alighting and the recovery path now require dry terrain-level ground; only state-6 MedEvac patients may complete on the hospital roof. **Rooftop spawn slope fixed 2026-08-09:** each rendered roof candidate must meet `normal.Z >= 0.99`; pitched points retry and no flat result fails without a street fallback. **Roof DECORATIONS excluded 2026-08-12:** a water tank, stair head or air-conditioning box is flat on top, so the slope rule waved them all through — the roof post now resolves the deck from 17 probes and takes the LOWEST supported band (a decoration always stands *on* the deck, so widest-band loses to a big one), and every candidate must have all eight of its own footprint probes within 20 cm of it. **Harness pickup locked 2026-08-09:** a deployed rescue harness may collect a rooftop survivor while the airframe is too high for cabin boarding; BHAV 305/opcode 48 rides the rope end and opcode 58 transfers the survivor when raised. **Passenger exit placement fixed 2026-08-09:** shared alighting now resolves the real seat row and places the person one body clearance outside the rendered fuselage; the mission layer no longer adds a second world-space spread.
- [SimCopter Speeder and burglar-car pursuits](simcopter-speeder-pursuit.md) — ambient Speeder is an ordinary car carrying flag 0x800 and events 0x21/0x26, not the 0x4000 Burglar mission; the searchlight IS the CARROBBR mechanic (obj[0x11b] is an illumination counter), and FUN_004b89a0 isn't in the Ghidra exports — read `.rdata`.
- [SimCopter mission authenticity pass](simcopter-mission-authenticity.md) — the "do we have X?" answers, from the shipped data. **§2 now carries the ARLU clip table MEASURED from the pose data (2026-08-12)** instead of inferred from bind sites: the 18 mnemonics in their real file order (the order written here before was wrong), and per clip how far the legs/torso/arms actually travel. `Wave` swings arms *and* legs 25 units — a whole-body flail, so "a rioter kicking a leg in the air" is that clip rendering correctly — while `WvNo` raises one arm with zero leg motion. **ARPP z is DOWN**; read it as up and a greeting measures as a kick. **No program makes people flee a descending helicopter** (that reconstruction is gone) and boarding is a 5-unit height band, never a Parked state. `Yumm` is dead art but **the kiss is real — voice event 39**, and the street chat's nine voice lines map through a NON-identity switch. **Opcode 60 is projectile type 4, not type 10**: the arsonist's firebomb, which burns 60 s then rolls `1 in (8 - difficulty)` for a building fire — and 1 rioter throw in 9 is the same op, so that is where riot fires come from. Water hitting people is interaction **mode 4** (BHAV 908, −1 agitation, 1-in-6 backfire of +8); the remake had the douse half of `FUN_00490690` and not the people half. **Riot traffic jams and the ordered megaphone→water→teargas bonus are in no shipped data** — the escalation incentive is emergent. Fire rescue and pre-emptive criminal arrest both already work; ambient criminals never existed. **Difficulty audit:** `_DAT_004f9740` IS `GetDifficultyTier()` (city field 0 **+1**), and 12 of its 15 consumers were already ported — but the three that were missing were all difficulty-scaled *mission sources*: the building-fire size/occupancy filter (tier 1 = 1x1 shacks only), the car-fire→medevac roll `1 in (0x40>>tier)`, the special-hits-ordinary car-fire roll `1 in (0x200>>tier)`, and the blocked-emergency-vehicle jam roll. **The XBLD property table `DAT_00504848` is now EXTRACTED from SimCopter.exe's `.data`** (statically initialised, so no live rip — parse the PE headers, map VA→file offset, and verify against known `.data` strings first; scripts in the scratchpad). Byte 0 is bit0 solid / bit1 building (57) / **bit2 has-people (39 ids, all >= 0x81)**; `+4..+0x10` is a person-emission anchor. The old stand-in was wrong both ways. It also exposes a shipped bug: the scheduled fire-rescue placer reads the tile id SIGNED, so it can never match an occupied building — the remake reads it unsigned on purpose.
- [Burning debris and the two fires it starts](simcopter-burning-debris-spread.md) — `FUN_0048ed00`'s class-0x10 burnout branches on whether the debris carries a live event id: -1 (the arsonist) opens a NEW fire and needs nothing burning nearby; a plane crash, a derailed carriage or a car burning out carries its record's id, ignites INTO that record and has **no** nearby-fire test. That owning arm is the game's fire-spread-from-wreckage and was entirely missing — `SpawnCrashDebris` took an EventId and dropped it. Ported 2026-08-11.

- [SimCopter fire/water FX](simcopter-fire-water-fx.md) — FIREPTS(0x120) = 22 POINT sprites not a mesh; effect cards are flat palette-coloured (no texture) with a radial soft-alpha material; rotor-wash = FUN_004881b0; fire/car-fire/bucket-douse/wind-kickback rendering added.
- [SimCopter flashing lights](simcopter-flashing-lights.md) — face type 25 = a 1-vertex blink marker whose material byte is a palette colour; FUN_00496c00 strobes the WHOLE world by colour on one 8-step global counter (phases 6/7 dark). Not face type 26, which is the effect markers. Two deliberate divergences: fixed world size (never distance-culled, floored at 1 physical output pixel) and a coloured point light on EVERY lit marker — uncapped, because the project renders with MegaLights.
- [SimCopter sound DECODED](simcopter-sound.md) — the 130-slot table (0x00-0x81) + the nine-call API, ported as `USimCopterAudioSubsystem`. Traps: ONE voice per id and Play-while-playing is a NO-OP (that is what stops water droplets machine-gunning); `AddFrequency` is absolute, not cumulative; attenuation is linear to only -40 dB at the 1920-unit (120 m) cull edge, and the cull is an octagon; sirens have no direction at all; CESSLP1 is the plane's engine and DIVE1 is the dive; the rotor loop is per model (COPLOOP2..6) and pitch, not volume, carries it. The old D2### dispatcher mapping was wrong — those ids are DIS053-068. **Radio decoded too:** playlists are globbed from disk (`*.id*`, `*.wav`), then run as a SHUFFLE BAG (two Fisher-Yates over a lagged-Fibonacci RNG, not MSVC rand) — every track plays once per cycle before any repeat — with probability gates per slot type (0 music always / 1 DJ 20% / 2 commercial 90% / 3 jingle 20%, jingle empty in retail) and a 10% back-to-back music repeat. **Two audio quality sets exist** (22050/16-bit retail vs 11025/8-bit), and because `AddFrequency` is relative to the clip's own rate the rotor pitches differently between them — check the asset rate before "fixing" it. `Reference/sound` was refreshed from the full install 2026-07-31; never copy the exe from one (SimCopterX-patched). **Radio ported 2026-07-31** as `USimCopterRadioSubsystem` + a dash4 tuner needle: the needle geometry is MEASURED from `DASH4.BMP` (lit band rows 24-30, cols 17-92; labels at x 22/38/53/69/84), not from the widget's stored rect (20,15,86,38), which bounds the whole head unit. Radio state reaches the dash over the "REIO" message id, defaulting to KMIX. **The missing DASH4 volume line was restored 2026-08-09:** original rect (98,19)-(110,39), marker x 100..101, logarithmic Y, bottom detent powers off; replacement art uses x -3, tuner y +4, and rocker travel 20..41. Volume input never reapplies the station; a channel change resets volume to full. Radio volume changes update the active `RadioComponent` immediately while preserving master-volume scaling, instead of waiting for the next track. Restore station before saved volume, and paint live radio state, so the rocker and audible gain cannot disagree at startup.
- [SimCopter vertex animation via WPO](simcopter-vertex-animation-wpo.md) — animate/displace city mesh verts with a material World Position Offset (`M_SimCopterWater`), NOT CPU `UpdateMeshSection`; GPU-only, works in editor+game; bake control data into vertex colors.

## Camera, materials and appearance

- [Overhead geometry is not ground](simcopter-overhead-geometry-collision.md) — the pedestrian walk probe and the flight model's `heli[0x59]` probe both started above the world and took their topmost hit, so every mesh acted like a solid box from its highest point to the terrain: people (police mid-chase especially) were walled in under bridge arches and power spans, and the helicopter was shoved in mid-air. Probe from the body, and ask the real mesh about walls.

- [SimCopter camera ground lift](simcopter-camera-ground-lift.md) — raising the boom PIVOT walks the helicopter down the screen; the old ramp only hit full strength at zero clearance, which never happens, so a landed helicopter got 25 cm of a 250 cm lift. The spring arm's own `bDoCollisionTest` must stay OFF — it swept from the roof to the socket-offset camera and clamped it, making the lift look dead however it was tuned. Whatever is under the camera is its ground, buildings included — a line trace STOPS at its first blocking hit, so skipping buildings to reach terrain beneath just reported "no ground" over an airport. Live on the debug panel's GROUND LIFT row, with a clearance/lift readout. `CameraDefaultZoomAlpha` is both the start zoom and the framing reference — keep them equal.



- [SimCopter exposure scale](simcopter-exposure-scale.md) — the `CelestialVaultDaySequenceActor` runs the sun at 120,000 lux where the old day/night actor ran it at 4, so every unlit effect card went BLACK and every gameplay light went invisible — nothing about them changed, the scale under them did. **The sun model has a hole at dawn/dusk (fixed 2026-08-12)**: its cosine is against FLAT GROUND and collapses while the sky is still bright, so the particles went black for that window; the emissive now takes `max(sun model, measured average scene luminance)` off a scene view extension, which is continuous and needs no calibration. Emissive goes through `EyeAdaptationInverse` and lights through `InverseExposureBlend`; unitless x 16/10000 = candelas, so a "650,000" searchlight is a hand torch.
- [SimCopter sprite-card lighting](simcopter-sprite-card-lighting.md) — **trees, ground and water now take roughness/specular/an ALBEDO CEILING per family from `MPC_SimCopterDayNight`, live off nine `SimCopter.Shading.*` CVars** (2026-08-07): the trees blew out to white in sun because a near-1.0 palette texel under a 120,000-lux sun clips by itself AND `CardNormalUpBias` 1.0 points every card at the sun at once, so the shared 0.3 specular lit the whole canopy. The ceiling must be on albedo (a material cannot clamp its own lit result) and is hue-preserving. Water had been sharing the dirt's matte numbers. To re-tune the tree material you must DELETE it first — it is `create_if_missing`, and the re-parent pass cannot rescue instances a delete leaves on null. Also: trees/signs (Maxis face type 2) were the only unlit city surface, so under the day sequence they read INVERTED: dark at noon, glowing at midnight. Now `M_SimCopterLitSpriteTexture`, masked + Default Lit, with the card normal biased to world up (crossed vertical quads would otherwise go black under a high sun). Fire and particle kernels stay unlit on purpose. **The privanim figure heads joined it 2026-08-06** with `CardNormalUpBias` 0 (a head is a ball with real normals), so no code sets a brightness for a head any more — a computed one was glowing before it had a floor and BLACK after.
- [SimCopter night lighting DECODED](simcopter-night-lighting.md) — **`DAT_004f9720 == 1` is NIGHT** (closes the old open question). Lit windows are NOT geometry: `FUN_004606d0` copies `skydark.bmp` images 1..5 over live atlas pages 2/39/40/20/13, and those five are sky.bmp's in BOTH states, never SIM3D's. Face type 11 is the light *cards* (street lamps `LAMP35..38`, headlights, rotor discs) — 550 of them and not one on a building. Ported as an `MPC_SimCopterDayNight` the atlas material samples: `NightBlend` plus a random lit roll **per TILE** (30% lit, 5% whole floors, re-seeded every sunset). The window mask is now **PAINTED** — `Content/NightWindows/windows_page_<atlas page>.png`, R=window, G=per-window id, B=per-row id, imported linear; the derived luminance mask survives only as the fallback for an unpainted page. **Lumen: the `#ifdef LUMEN_CARD_CAPTURE` guard is intact and covers both of 5.8's card shaders (incl. the Nanite compute one) — but it only ever covered the surface cache, and Lumen's SCREEN TRACES see the windows as the base pass drew them. That half is `r.Lumen.ScreenTracingSource`, now 1.** **Also the big trap: `M_SimCopterSpriteTexture` is unlit with a baked 26000-nit `EmissiveNits` default, and six consumers create MIDs from it — a consumer that forgets the parameter glows day and night.** That was the fire, the pedestrians and every figure head at once; `USimCopterEffectExposureSubsystem::ApplyEmissiveNits` is now the only way to write it, with a light-source/surface distinction (surfaces get NO minimum, so they go dark with the sun). The hangar's `ui+0x112` is "is it DAY", the inverse of the global.
- [SimCopter Low Power Graphics](simcopter-low-power-mode.md) — the Settings checkbox that makes the city run on an iGPU: Low scalability + a CVar table of ONLY what scalability misses + runtime light cuts. Traps: MegaLights is on via the level's post process volume so `EnableForProject` cannot switch it off (`r.MegaLights.Allowed` can); `GlobalIlluminationQuality@0` stops Lumen lighting but leaves the Lumen scene running; the Low profile leaves bloom at 4; and the atlas pages have no mips, which is why texture quality Low is nearly free here. **Forward shading was built and reverted** — read that section before re-attempting it: the game crashes past the main menu, the editor asserts in the base pass, and the user's `Engine.ini` is not viable storage for ANY restart-required setting because the editor deletes it on shutdown.
- [SimCopter shared vehicle material](simcopter-vehicle-material.md) — `M_SimCopterLitVertexColor` is shared by the vehicles AND the city; only the vehicles go through `USimCopterVehicleMaterialSubsystem`'s instance, so a metallic helicopter does not chrome the skyline. `SetScalarParameterValue` on a parameter the material lacks is silently ignored — check the asset before debugging a dead slider.

## UI

- [The cockpit UI may not take the keyboard](simcopter-ui-keyboard-focus.md) — REMAKE-ONLY (2026-08-25). Gameplay bindings only fire while the GAME VIEWPORT holds Slate focus, and one focusable widget over the viewport takes it on a single click — then `UGameViewportClient::LostFocus` releases whatever the player was holding, which is "clicking a button stopped W". Cockpit widgets are descendants of `SViewport`, so as long as NONE of them accepts focus the leaf-to-root walk lands back on the viewport and nothing moves; `SSimCopterToolFlaps` (whose `Ctrl+Alt+M` handlers were already dead — the chord is a pre-processor) and the check-up buttons were the two that did. Plus `bShouldFlushPressedKeysOnViewportFocusLost=False` (the engine ORs over GameAndUI's own "don't flush for in-game UI" decision; Alt-Tab is covered explicitly instead) and a Slate input pre-processor that takes the keyboard back — in time for the key being pressed, because pre-processors run before the focus path is read. A DROPDOWN drops focus on the WINDOW, not the viewport. `SimInputFocus` prints the whole decision.
- [The collective IS the engine control, and the takeoff prompt](simcopter-collective-is-the-engine.md) — REMAKE-ONLY (2026-08-25). The `SimCopterEngineStart`/`SimCopterEngineShutdown` actions are retired: they shipped on the very keys `SimCopterCollective` already used, so Controls listed four rows for two keys and a rebind could split one control in half. `MoveCollective` derives both holds from the axis at the same 0.25 threshold `BuildFlightInputs` uses — which is what the gamepad always did. A stale saved `Input.ini` is filtered by `IsRetiredMapping`. Plus a 5-second "Hold the {collective up} to take off, or press {Interact} to get out" prompt above the dash, both keys read from the live `UInputSettings`: its trap is that `PossessedBy` must NOT seed the taken-off latch from `bIsLanded` (written by `SimulateFlightStep`, so stale on the possession frame — a stale `false` kills the prompt forever).
- [Replay clips, the free camera and the Tab panel](simcopter-replay-clips.md) — WHOLE-CLOTH ADDITION (2026-08-13), no `FUN_004xxxxx` behind any of it and no 16.16. A **snapshot** recorder at 20 Hz with a sparse key per actor, NOT deterministic re-simulation — so scrub/reverse/slow-mo cannot desync, and a clip is a recording that never re-runs the sim. The decision track comes free by teeing `SIMCOPTER_PEOPLE_TRACE` (every shipped VM decision site already calls it) and `PushMissionLogMessage`, attributed by a scoped source opened in the agent's Tick. **A review freezes the world with near-zero GLOBAL TIME DILATION, never `SetPause`** — a paused world stops COMPONENT ticks, so `USpringArmComponent` never recomputes its socket and the camera stays frozen while the actors move correctly underneath it (indistinguishable from "the clip doesn't play"; the free camera dies the same way, and pausing also raises the engine's PAUSED overlay). The playhead, the free camera and `UpdateCameraForReplay` then have to run on REAL time, and boom lag is forced off. **Audio is recorded and replayed too** (`SoundStart`/`SoundStop` events with slot id + flags + position; zero position = a 2D play): a review silences the live mixer, because a frozen world never stops its own rotor loop, sirens, radio or per-person walking voices. Sounds fire only on forward continuous playhead motion — scrubbing re-anchors silently — and a per-frame `SoundMove` snapshot is required or a loop stays pinned where it started (that is why the rotor sounded like it was next to the listener). **Particles need BOTH halves**: the pools run on `GetPresentationDeltaSeconds` (real time during a review) AND the creator calls are recorded and re-issued, because particles are spawned by gameplay and gameplay is frozen — advancing pools with nothing spawning just lets the existing particles finish. Only the OUTERMOST creator call is recorded (`SpawnEffect` makes its own puff, `SpawnRing` makes N particles) or playback doubles every effect. **`UpdateRopeVisuals` places the rope/bucket/harness in WORLD space, not parented** — so a review leaves them where the take ended while the aircraft flies off; the clip records the winch node + flags and `ApplyReplayWinchState` rebuilds the rope from the replayed anchor each frame. The tear gas pool is its own spawner (`Launch`) and needs its own recording; airframe impacts already ride the effect track via `SpawnHardLanding`. **One live actor can back SEVERAL tracks** — boarding destroys the on-foot pawn and leaving spawns a new one, so a round trip makes two on-foot tracks that both bind to the pawn that exists now; `ApplyPlayhead` drives in one pass and hides in a second, or the dead track hides the actor the live one just placed (the "pilot is invisible" fault). while the mission/traffic/ambient system actors get their tick disabled outright so no callout lands over a clip. Traps: `BeginPlay` starts the behaviour VM so puppets MUST spawn deferred; the live crowd is hidden and restored *before* the world is thawed or the helicopter teleports on exit; the airframe's bank lives on `ModelPivot`, not the actor rotation; angles must interpolate the short way. Clips live in a folder per level, which is what enforces "only this city's clips". Free cam reuses the flight axes and therefore blocks the pawn's input while live. **Tab is show/hide for the CONTROLS, never a mode change** — a take keeps recording and a review keeps playing with the panel down (that is how you watch a clip full screen with the free camera), so `IsFreeCameraActive` asks `IsReplayModeActive()` not `bPanelOpen`; CLOSE ends a review keeping the clip, RESET is the destructive one. Four more traps found the same day: Slate eats Tab for focus navigation so the panel must answer it itself; the view target must leave the free camera *before* the actor is destroyed; closing the panel restores the view to the POSSESSED PAWN (routing through `ApplyCameraView` pointed an on-foot player at their parked helicopter); and `EnableInput` must go back to the pawn that was actually blocked. **Two ways this feature killed all gameplay input, both now guarded.** (1) A controller-side binding that shares a key with gameplay MUST set `bConsumeInput = false` — "controlled pawn gets last dibs on the input stack", so the controller is processed first and consumes by default; reusing the flight axes for the free camera ate W/A/S/D/Space/Ctrl/mouse/C/RMB with the panel *closed*. `SimCopter.Replay.InputDoesNotConsume` guards it and was verified to fail when reintroduced. (2) A viewport-content widget that accepts keyboard focus stops the player flying — gameplay axes only fire while the GAME VIEWPORT holds focus; the panel is now mouse-only with no `SupportsKeyboardFocus` and no `OnKeyDown`, every shortcut being a controller binding. It swallows the MOUSE the same way, hence `SelfHitTestInvisible`. Every transition logs to `LogSimCopterReplay` at Log by default, plus `SimReplayStatus` (which prints the focused widget first, because that is the answer most of the time).
- [SimCopter save/load](simcopter-save-load.md) — the Settings Save/Save As flow, the main-menu career/user Open pickers, and a versioned Unreal SaveGame archive. Format v3 adds the live clock and per-save Time of Day settings to v2's BOMB-equivalent missions/fires, city demolition, people/traffic/dispatch, ambient vehicles, aircraft/flight/camera/tools/projectiles/passengers, user-city tuning, and on-foot possession. Stable identities survive repeated saves; this still does not claim `.scc`/`.scu` byte compatibility.
- [Advancing between career cities](simcopter-career-city-transition.md) — `FUN_00408210` clears **only** the score (+0x50); money, fleet, fittings and tear gas all cross a career city boundary, and `FUN_00484790` re-services every airframe on its pad (full tank, full hit points, depreciation zeroed) so fuel/damage must NOT be carried. The remake ran `FUN_00407f30`'s new-career reset on every city, wiping the lot. Three traps: `SwitchHelicopterModel` normalises the old fuel/damage fractions so full tank/HP must be written *after* it; an advancement gets **no Cancel** (`screen[0x2e]`), and backing out stranded the career; and city 29's all -1 successor trio means the last level ends the ladder rather than continuing it.
- [SimCopter front end](simcopter-front-end.md) — main menu + career city select decoded and ported; the whole main-menu layout is ONE stack descriptor (`dump-asm` it), `FUN_0045f670` loads main5 into +0x214 and main4 into +0x218 (not the other way round), Esc on the main menu is Quit with no confirmation, and the career is a 12-level branching graph whose level field is in the exe, not career.twk. `FUN_0044d070` resolves the installed zero-byte `menusky.smk` stub to the CD: the authentic movie is 640x480, 201 frames at 71 ms/frame, and the remake now plays it centred while repeating its always-clear live sky crop across widescreen margins. No sliders anywhere in the front end — those belong to the in-game Settings screen.
- [SimCopter Settings screen](simcopter-settings-menu.md) — the in-game options menu (playmenu.bmp, 0x7d3) + City Settings / Sound / Options / Controls, decoded and ported. The menu descriptor has TWO variants and the **command base** absorbs the difference, so item ids stay stable; `DAT_00518d50 == 1` means a USER game, which is why City Settings comes and goes; the "Sound" dialog is the radio head unit and its channel-name strings are unused; the pause is REFERENCE COUNTED; and the Game Volume fader is the one horizontal slider in the game. Options groups Gameplay/Input/Graphics, keeps Time of Day per-save, and persists three FOV plus separate mouse/controller X/Y sensitivity values profile-wide; Controls still lists bindings because the keyboard picture's per-key rect table is not in the exports.
- [SimCopter Graphics page: DLSS, resolution scale and quality presets](simcopter-dlss-resolution-scale.md) — four linked bugs fixed 2026-08-05, two of them nasty ordering traps. **(1)** the deprecated `UDLSSLibrary::SetDLSSMode()` force-writes `r.ScreenPercentage` at the CVar's current priority, colliding with the Resolution Scale row/Low Power Graphics. **(2)** a manual Resolution Scale while DLSS is on **crashes** (`NGX_D3D12_EVALUATE_DLSS_EXT`, non-uniform ScaleX/ScaleY) — that row and the new Anti-Aliasing Method dropdown are now greyed while `IsDlssEnabled()`. **(3) THE BIG ONE: a `UGameInstanceSubsystem::Initialize()` runs BEFORE `OnPostEngineInit`, so it must never ask a plugin whether a feature is supported** — `IsDLSSSupported()` answered a false "no" and the init-time "sanitize" silently destroyed the player's DLSS/FrameGen/Reflex settings on every launch. **(4)** `GetOverallScalabilityLevel()` returns -1 (Custom) whenever the resolution scale is not the preset's exact value, and clamping it to 0 made Overall Quality report "Low" over an Epic mix.
- [SimCopter Check-up menu](simcopter-checkup-menu.md) — the landing-pad repair/refuel panel (strings 590-598); its Damage and Fuel sliders are in DOLLARS and `heli[0x34]` is HIT POINTS, not damage; servicing off-airport costs triple, but only the repair applier divides it back. **Layout comes from `dump-asm`, not from measuring the bitmap** — the decompile aliases all sixteen control rects but the assembly states each one, plus its font size and justification; that generalises to every dialog. Slider art is SLIDERTV.BMP (thumb) over the page's printed track, drawn by a hand-rolled widget because `SSlider` rotates a vertical thumb 90°.
- [SimCopter cockpit map](simcopter-cockpit-map.md) — dash5 is a 124x98 palette-INDEX raster (fires animate by counting up the palette, ground shades by an index add), not drawn primitives; zoom is a shift and the grid rows are asymmetric (sub-row 1 at zoom 2, last at zoom 3); icons are SIM3D pages 3/12 read as raw indices, not via FMaxisTextureReader (it flips); mapbttn is four 16px columns, so a right-hand button's pressed cell is the fourth; the original's instrument strip is bottom-LEFT and the map fills the right edge above it; `=`/`-` zoom, from input.cfg.
- [SimCopter map selection + Base Location](simcopter-map-selection-base-location.md) — the map's selection `DAT_0057f9d8` lives in the MISSION LAYER: adopted only when null, re-picked (first live slot) only when the SELECTED record completes, left STALE on category-4 failure/jam expiry, never touched by pickup/delivery. Mask **0x100000 is the "Base Location" record** (string 586) city entry `FUN_0047a240` creates at airport origin+1 — not a UFO. **Transport: +0x28/+0x30 = destination, +0x38 = pickup** (`FUN_004abb30`, cleared once all picked up); `FUN_004a4000(0x40)` = -1/3.
- [SimCopter hangar shell](simcopter-hangar-shell.md) — catalog/log/inventory ported; the shell's TEXT is in the exe's Win32 STRINGTABLE (NOT the Ghidra `.rdata` export); the hangar belongs on the airport's TERMINAL plot (demolish 0x096+0x165 first or the base slab z-fights); its skin is SIM3D page 40 cells 20-23/61; all nine `CAT_*T` tab strips share one hit-rect table; overlap events miss a teleported pawn.
