[/Script/SimCopterRemake.SimCopterSettings]
; First-run default on Mac only; a player's saved choice overrides it. Measured on an M1 at 720p
; (Docs/memory/mac-performance.md): Epic, even with the Mac device profile, runs ~76 ms a frame
; (~13 fps) over the city, and every preset above Low stays above 40 ms because the volumetric
; clouds alone cost ~19 ms on Apple GPUs. Low Power Graphics runs the same view at ~10 ms.
bLowPowerMode=True
